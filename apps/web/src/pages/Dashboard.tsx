export function Dashboard() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-2">Painel operacional</h1>
      <p className="text-slate-400">
        Painel operacional da Defesa Civil — risco atual, sensores simulados,
        alertas ativos e abrigos pendentes. Conteúdo real chega quando o
        motor de risco canônico e a API de telemetria estiverem ligados
        (F3/F4).
      </p>
    </div>
  );
}
